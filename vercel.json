{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "incoming": ["VITE_API_BASE_URL", "VITE_ENV"]
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
